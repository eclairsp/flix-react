import React from "react";
import posed, {PoseGroup} from "react-pose";
import "./notify.css";

const Notify = () => {
    return (
        <PoseGroup>
            <div className="not">Hi</div>
        </PoseGroup>
    );
};

export default Notify;
